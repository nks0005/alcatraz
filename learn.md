# Alcatraz 
> `KVM을 더 낮은 권한`으로 격리(sandbox)하기 위해, 자체 pico-hypervisor(Hyper-box)가 `Ring -1(호스트 하이퍼바이저)`을 차지하고 `EPT/VMCS-shadowing/하드웨어 브레이크포인트`로 `탈출 행위(프로세스 생성, 권한 상승, 코드 변조 등)`를 탐지/차단 하는 커널 모듈 기반 프로젝트

# 레포 구조 
> Source/ : 핵심 소스/빌드
    > Source/hyper_box/ : 메인 모듈 `hyper_box.ko` 
    > Source/hyper_Box_helper : 보조 모듈 `hyper_box_helper.ko`




hyper-box + tailored kernel



1. 기존 KVM 기반 격리의 핵심 약점
- KVM -> Ring -1(하이퍼바이저 권한)에서 동작 -> KVM 취약점이 존재하면 공격자가 Ring -1에서 직접 탈출이 가능한 구조
---> KVM/QEMU 자체가 공격 표면이 될 수 있음


더 높은 권한으로 검사? Ring -2
--> SMM(Ring -2) 장악 후 하이퍼바이저를 모니터링 ? -> BIOS/UEFI 펌웨어 수정이 필요


# SMM
SMM(System Management Mode)은 x86 CPU에 있는 특수한 실행 모드

x86 CPU란, 인텔이 개발한 8086 마이크로프로세서를 시작으로 발전해온 일련의 명령어 집합 아키텍처(ISA, Instruction Set Architecture)를 의미합니다. 

일반적으로 우리가 사용하는 데스크탑, 노트북, 서버의 CPU(중앙처리장치)는 대부분 x86이나 x86-64(64비트 확장) 아키텍처를 기반으로 합니다. 
x86 아키텍처의 주요 특징은 다음과 같습니다:

- CISC(복잡한 명령어 집합) 구조: 다양한 연산을 한 번에 수행할 수 있는 복잡한 명령어들을 포함합니다.
- 레거시 지원: 16비트, 32비트, 64비트 모드를 모두 지원하여, 매우 오랜 기간 소프트웨어 호환성을 유지합니다.
- Ring 구조(권한 레벨): 보안 및 안정성을 위해 0(Ring 0, 커널)~3(Ring 3, 사용자)까지 4단계의 권한 레벨을 제공합니다. 하이퍼바이저(Ring -1), SMM(Ring -2)이라는 특수 권한 레벨도 x86 아키텍처에서 구현됩니다.

요약하면, x86 CPU는 PC 등에서 주로 사용하는 거의 모든 인텔/AMD 프로세서의 기반이 되는 명령어 집합 및 구조를 뜻합니다.


Ring -2?
- Ring 3 (유저), Ring 0 (커널)
- 가상화 추가 -> 하이퍼바이저를 관용적으로 Ring -1 
SMM = Ring -2 


SMM 실행되는 조건
- 특정 이벤트가 발생 -> CPU가 SMI(System Management Interrupt)를 받으며 SMM으로 돌아감
- CPU 는 SMRAM 이라는 보호된 메모리 영역에 있는 SMM 핸들러 코드를 실행
-> 끝나면 원래 실행 흐름으로 돌아옴

>> 



## 예시: 팬 속도 제어(열 관리)로 SMI/SMM이 도는 흐름
> 아래는 "전형적인 플랫폼"에서 일어날 수 있는 흐름 예시이며, 실제 트리거/레지스터/정책은 메인보드/펌웨어 구현에 따라 달라질 수 있음

1) CPU/보드 온도 상승
- 온도 센서(예: CPU 패키지/메인보드 센서)가 임계치에 근접/초과

2) 플랫폼 컨트롤러가 이벤트를 감지
- 칩셋(PCH) 또는 EC(Embedded Controller)가 센서 값을 보고 "열 관리 이벤트"로 판단

3) SMI 발생
- 플랫폼이 CPU에 SMI(System Management Interrupt)를 발생시켜 SMM 진입을 유도

4) CPU가 SMM으로 전환
- 현재 실행 컨텍스트(레지스터 등)를 저장하고 SMRAM으로 진입

5) SMM 핸들러(펌웨어 코드) 실행
- "왜 SMI가 왔는지" 상태/원인 레지스터를 확인(열 이벤트 여부 확인)
- 팬 제어 정책에 따라 팬 PWM/컨트롤 레지스터 값을 변경(팬 속도 증가)
- 필요하면 추가 조치(스로틀링 요청/임계치 재설정/이벤트 플래그 클리어)

6) RSM으로 복귀
- `RSM`으로 원래 실행 흐름(하이퍼바이저/커널/유저)로 돌아감
