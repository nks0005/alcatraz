# Copy Fail (CVE-2026-31431) — 핵심 요약

1. **파일은 메모리(페이지 캐시)에 올라감**  
   - 디스크가 아니라 RAM에 캐싱된 내용으로 읽기·실행이 이뤄짐

2. **`splice` + AF_ALG(Copy Fail) 버그**  
   - `splice`로 캐시 **페이지**를 암호 **쓰기 버퍼(dst)** 에 연결 (제로카피)  
   - `authencesn` 복호화가 그 캐시에 **4바이트** 기록 → **디스크 파일은 그대로**

3. **setuid 바이너리로 root**  
   - 예: `/usr/bin/su` — 캐싱된 메모리에서 4바이트 패치 후 **실행**  
   - **4바이트** = 실행 경로 변경 / **setuid exec** = euid=0 (root)

4. **결과**  
   - 메모리에 올라온 **캐싱된 파일**을 조작한 뒤 실행 가능  
   - **디스크 기반 무결성 검사**(sha256, AIDE 등)는 **통과할 수 있음** (디스크만 보므로)  
   - 재부팅 시 캐시 변조는 사라짐

---

## 한 줄

> 캐시된 setuid 바이너리 RAM만 4바이트 패치 → 실행 → root (디스크는 깨끗해 보임)

---

## 보충 정리 (개념·Q&A)

### 페이지 캐시

- OS는 파일을 **읽을 때마다 디스크에서 매번 읽지 않음** → **페이지 캐시(RAM)** 에 올려 두고 재사용.
- `read()`·`exec`는 **디스크 파일 자체**가 아니라 **메모리에 캐싱된 내용**을 봄.
- Copy Fail은 **디스크가 아니라 이 캐시**를 건드림.

### `read()` vs `splice()`

| | `read()` | `splice()` (Copy Fail 경로) |
|---|----------|------------------------------|
| 동작 | 캐시 → **유저 버퍼로 복사** | 캐시 페이지를 **참조로 연결** (제로카피) |
| 결과 | **복사본** (캐시와 분리) | **같은 캐시 페이지** |
| 공격 | 이 경로로는 **캐시 직접 수정 불가** | AF_ALG dst로 **캐시가 연결됨** |

### AF_ALG란?

- **AF** = Address Family, **ALG** = Algorithm → **암호 알고리즘용 소켓**.
- 일반 사용자도 `socket(AF_ALG)` + `bind("aead", "authencesn(...)")` + `sendmsg`로 **커널 암호 API** 호출.
- Copy Fail에서 **파일 캐시를 암호 경로에 끌어들이는 입구**.

### 공격 흐름 (한 번 더)

```
파일 → 페이지 캐시(RAM)
  → splice: 캐시 페이지를 AF_ALG dst에 연결
  → authencesn 복호화: dst에 4바이트 기록 (= 캐시 내용 변경)
  → exec setuid (/usr/bin/su 등): euid=0 + 변조된 캐시 코드 → root
```

- **`splice`만**으로 4바이트를 쓰는 게 아님 → **`splice` + `algif_aead` in-place 버그** + **`authencesn`**.

### 4바이트와 root

| 역할 | 담당 |
|------|------|
| **4바이트** | 캐시된 **기계어 일부** 변경 (실행 경로·점프 등). 포인터 주소 변경이 아니라 **내용** 수정. |
| **root** | **setuid** root 소유 바이너리 **exec** → 커널이 **euid=0** 부여. |

- 4바이트 값은 **완전 임의가 아닐 수 있음** (AAD `seqno_lo` 등). **임의 전체 수정**이 아닌 **제한된 LPE**.

### 왜 “쓰기 버퍼(dst)”가 될 수 있나? → **정책이 꼬임**

서브시스템마다 **같은 물리 페이지**에 다른 규칙이 겹침:

| 쪽 | 기대 |
|----|------|
| **파일(VFS)** | 페이지 캐시 = 파일 내용, **함부로 변경 X** |
| **`splice`** | fd 사이로 **페이지 넘기기** |
| **`algif_aead`** | in-place 최적화 → **src = dst**, dst는 **써도 됨** |
| **`authencesn`** | 복호화 시 dst에 **4바이트 스크래치 기록** (정상 동작) |

**버그:** `splice`로 **파일 캐시**가 암호 **dst**로 등록됐는데, **“파일 백드 페이지는 dst로 쓰면 안 된다”** 검사가 없음.  
**`authencesn`**이 보호를 “뚫은” 게 아니라, **이미 잘못 연결된 dst**에 **원래 하던 대로** 쓴 것.

**한 줄:** 성능 최적화(zero-copy, in-place)로 **정책 경계가 무너졌고**, 패치는 **입·출력 버퍼 분리** 등으로 정책을 다시 맞춤.

### 기본 원리 (최종)

1. 파일은 **메모리(페이지 캐시)** 에 올라감.  
2. **`splice` + AF_ALG(Copy Fail)** 로 캐시를 dst에 붙이고 **4바이트** 기록 (디스크 무변).  
3. **setuid** 바이너리(예: `/usr/bin/su`) **실행** → root.  
4. **디스크 무결성 검사**는 통과할 수 있음 / **재부팅** 시 캐시 변조 소멸.
